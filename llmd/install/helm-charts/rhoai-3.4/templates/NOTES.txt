RHOAI 3.4 additional operators for the llm-d distributed inference course.

The following operators are already installed by the cluster provisioning:
  - Node Feature Discovery (NFD)
  - NVIDIA GPU Operator
  - OpenShift Serverless (Knative)
  - Authorino
  - OpenShift Pipelines
  - Red Hat OpenShift AI (RHOAI)

This chart installs the remaining dependencies:
{{- if .Values.servicemesh3.enabled }}
  - Red Hat OpenShift Service Mesh 3 — openshift-operators
{{- end }}
{{- if .Values.keda.enabled }}
  - Custom Metrics Autoscaler (KEDA) — {{ .Values.keda.namespace }}
{{- end }}
{{- if .Values.monitoring.userWorkload.enabled }}

User Workload Monitoring: enabled via cluster-monitoring-config ConfigMap
{{- end }}
