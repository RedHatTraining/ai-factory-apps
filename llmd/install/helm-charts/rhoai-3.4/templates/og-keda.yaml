{{- if and .Values.keda.enabled (not (lookup "operators.coreos.com/v1" "OperatorGroup" .Values.keda.namespace "openshift-keda")) }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-keda
  namespace: {{ .Values.keda.namespace }}
  labels:
    {{- include "rhoai.labels" . | nindent 4 }}
spec: {}
{{- end }}
