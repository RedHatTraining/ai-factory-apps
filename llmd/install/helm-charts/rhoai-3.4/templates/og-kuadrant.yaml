{{- if and .Values.kuadrant.enabled (not (lookup "operators.coreos.com/v1" "OperatorGroup" .Values.kuadrant.namespace "kuadrant-system")) }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: kuadrant-system
  namespace: {{ .Values.kuadrant.namespace }}
  labels:
    {{- include "rhoai.labels" . | nindent 4 }}
spec: {}
{{- end }}
