apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: grafana-monitoring
spec:
  config:
    security:
      admin_user: admin
      admin_password: admin
  route:
    spec:
      tls:
        termination: edge
